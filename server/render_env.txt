# Copy these lines (edit values) and paste into Render service Environment -> "Add Environment Variable" or use bulk import.
MONGO_URI=your_mongo_connection_string_here
JWT_SECRET=replace_with_a_strong_random_secret
JWT_EXPIRES_IN=7d
ADMIN_EMAIL=admin@minidmart.com
ADMIN_PASSWORD=Admin@123
CLIENT_URL=https://your-frontend-url.vercel.app
NODE_ENV=production

# Optional email settings (leave blank if not used)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
